# Data Ownership

Identity owns credentials and authorization. Account owns customer and account metadata. Ledger owns balances, entries, transactions, reservations, and idempotency. Payment owns payment state. No service reads or writes another service database.
