{
  "name": "@corebank/config",
  "version": "1.0.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit",
    "lint": "eslint \"src/**/*.ts\"",
    "test": "jest --config ../../jest.config.cjs --roots src --passWithNoTests"
  },
  "dependencies": {
    "zod": "^3.24.1"
  }
}
